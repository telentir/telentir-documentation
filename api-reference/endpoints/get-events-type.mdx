---
title: 'GET /api/events/{type}'
description: 'Subscribe to event stream'
openapi: "api-reference/openapi.json GET /api/events/{type}"
---

## Try it

```bash
curl https://app.telentir.com/api/events/{type} \
  -H "Authorization: Bearer $TELENTIR_TOKEN"
```

## Body

_This endpoint does not accept a request body._

## Response

- **200** - SSE stream
