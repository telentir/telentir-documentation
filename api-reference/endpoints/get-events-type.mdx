---
title: 'Get Events'
description: 'Stream Redis-backed Server-Sent Events.'
---


<div class="endpoint-heading">
  <span class="method-chip method-get">GET</span>
  <span class="endpoint-heading__label">Get Events</span>
</div>
<div class="endpoint-grid">
  <section>

### Endpoint

| Method | Path |
| ------ | ---- |
| `GET`  | `/api/events/{type}` |

Opens an SSE connection for key/object/publish events via `RedisEventStream`.

## Authorization

- Requires bearer token / API key with phone verification.

## Path parameters

| Param | Type | Description |
| ----- | ---- | ----------- |
| `type` | string | One of `key_created`, `key_deleted`, `object_created`, `object_updated`, `object_deleted`, `publish`, `unpublish`. |

## Query parameters

Any search params are forwarded as filter fields (e.g., `?objectId=obj_123`).

## Body

_Not applicable._

## Response

Content-Type `text/event-stream` with data payloads such as:

```text
event: message
data: {"id":"obj_123","user_id":"user_123","key_id":"key_123"}
```

<details class="try-it">
  <summary>Try it</summary>

```bash
curl https://app.telentir.com/api/events/object_created?objectId=obj_123 \
  -H "Authorization: Bearer $TELENTIR_TOKEN"
```
</details>

  </section>
  <aside>
    <h3>Examples</h3>
    <h4>Node.js (EventSource)</h4>

```javascript
const es = new EventSource('https://app.telentir.com/api/events/object_created', {
  headers: { Authorization: `Bearer ${token}` },
});
es.onmessage = (event) => {
  console.log(JSON.parse(event.data));
};
```

    <h4>Sample event</h4>

```text
data: {"id":"obj_123","user_id":"user_123","key_id":"key_456"}
```
  </aside>
</div>

<style>
.endpoint-heading {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  margin: 1.5rem 0 1rem;
}
.endpoint-heading__label {
  font-size: 1.75rem;
  font-weight: 600;
}
.method-chip {
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: 0.08em;
  padding: 0.15rem 0.85rem;
  border-radius: 999px;
  text-transform: uppercase;
}
.method-chip.method-get {
  background: #d1fae5;
  color: #065f46;
}
.method-chip.method-post {
  background: #dbeafe;
  color: #1d4ed8;
}
.method-chip.method-put {
  background: #fef3c7;
  color: #92400e;
}
.method-chip.method-delete {
  background: #fee2e2;
  color: #b91c1c;
}
.method-chip.method-patch {
  background: #ede9fe;
  color: #6d28d9;
}
.endpoint-grid {
  display: grid;
  gap: 1.5rem;
  grid-template-columns: minmax(0, 2fr) minmax(0, 1fr);
}
.endpoint-grid aside {
  border-left: 1px solid var(--mint-border, #e5e7eb);
  padding-left: 1rem;
}
details.try-it {
  margin-top: 1rem;
  border: 1px solid var(--mint-border, #e5e7eb);
  padding: 0.75rem 1rem;
  border-radius: 0.5rem;
  background: var(--mint-bg-muted, #fafafa);
}
details.try-it > summary {
  cursor: pointer;
  font-weight: 600;
}
</style>
