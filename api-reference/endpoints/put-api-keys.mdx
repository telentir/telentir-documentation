---
title: 'PUT /api/api-keys'
description: 'Create a new API key.'
---

<div class="endpoint-grid">
  <section>

### Endpoint

| Method | Path |
| ------ | ---- |
| `PUT`  | `/api/api-keys` |

Generates a key with the `tel_` prefix via `database.apiKeys.create`.

## Authorization

- Requires bearer token or API key belonging to a phone-verified user (`auth({ requirePhoneVerification: true })`).

## Body

| Field | Type | Required | Description |
| ----- | ---- | -------- | ----------- |
| `name` | string | Yes | Friendly name to display in the dashboard. |

## Response

```jsonc
{
  "id": "key_123",
  "name": "backend-job",
  "key": "tel_a1b2c3...",
  "last_used_at": null,
  "created_at": "2025-11-13T16:30:00.000Z"
}
```

The `key` value is only returned once.

<details class="try-it">
  <summary>Try it</summary>

```bash
curl https://app.telentir.com/api/api-keys \
  -X PUT \
  -H "Authorization: Bearer $TELENTIR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name":"backend-job"}'
```
</details>

  </section>
  <aside>
    <h3>Examples</h3>
    <h4>cURL</h4>

```bash
curl https://app.telentir.com/api/api-keys \
  -X PUT \
  -H "Authorization: Bearer $TELENTIR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name":"backend-job"}'
```

    <h4>JavaScript (fetch)</h4>

```javascript
const res = await fetch('https://app.telentir.com/api/api-keys', {
  method: 'PUT',
  headers: {
    Authorization: `Bearer ${token}`,
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({ name: 'backend-job' }),
});
const data = await res.json();
```

    <h4>Sample response</h4>

```json
{"id":"key_123","name":"backend-job","key":"tel_a1b2c3","last_used_at":null,"created_at":"2025-11-13T16:30:00.000Z"}
```
  </aside>
</div>

<style>
.endpoint-grid {
  display: grid;
  gap: 1.5rem;
  grid-template-columns: minmax(0, 2fr) minmax(0, 1fr);
}
.endpoint-grid aside {
  border-left: 1px solid var(--mint-border, #e5e7eb);
  padding-left: 1rem;
}
details.try-it {
  margin-top: 1rem;
  border: 1px solid var(--mint-border, #e5e7eb);
  padding: 0.75rem 1rem;
  border-radius: 0.5rem;
  background: var(--mint-bg-muted, #fafafa);
}
details.try-it > summary {
  cursor: pointer;
  font-weight: 600;
}
</style>
